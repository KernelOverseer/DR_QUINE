#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#define FT(x) x (fd, #x")\n");close(fd);}

/*
   Commentaire
*/

FT(int main(void){int fd;fd = open("Grace_kid.c", O_WRONLY | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);dprintf(fd, "#include <stdio.h>\n#include <fcntl.h>\n#include <unistd.h>\n#define FT(x) x (fd, #x\")\\n\");close(fd);}\n\n/*\n   Commentaire\n*/\n\nFT(");dprintf)
